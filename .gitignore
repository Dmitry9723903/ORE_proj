# Сборка
bin/
obj/
artifacts/
*.user
*.suo

# Среда разработки
.vs/
.idea/
.vscode/

# Результаты прогонов
TestResults/
*.trx
*.coverage

# Локальное
*.local.json

# Настройки среды со значениями секретов. Пример лежит в .env.example и
# значений не содержит: D-83 запрещает секрет в репозитории, и одного
# запрета мало — файл, который заводят по образцу, обязан быть закрыт
# заранее. Найдено тем, что .env с паролем чуть не попал в коммит.
.env